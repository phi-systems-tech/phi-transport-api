phi-transport-api (2.1.0-1) trixie; urgency=medium

  * Contract 2.1.0: the management surface. TransportInterface gains
    describeManagement() - a summary line and actions in the adapter action
    shapes - and invokeAction(); CoreFacade gains completeAction() for the
    answer. management.h builds the JSON text. Both defaults do nothing, so
    a transport without anything to manage changes no code, but every
    transport is rebuilt: kTransportApiVersion is 2.1.0.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Sun, 06 Sep 2026 07:33:51 +0200

phi-transport-api (2.0.1-1) trixie; urgency=medium

  * phi-transport-host is a friend of TransportInterface, next to the
    TransportManager: the process that drives a plugin out of process makes
    the same calls, and the friend line is where the contract says who may.
    kTransportApiVersion stays 2.0.0 - nothing a plugin builds against moved,
    and every installed transport keeps loading.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Sun, 06 Sep 2026 07:19:00 +0200

phi-transport-api (2.0.0-1) trixie; urgency=medium

  * Contract 2.0.0: the plugin lives on a phi::runtime thread core provides.
    attachRuntimeLoop() hands it the Loop before attachCoreFacade() and
    start(); every interface call arrives on that loop's thread, and the
    plugin drives its own I/O with the loop's watches and timers. The
    package depends on libphi-runtime-dev instead of qt6-base-dev - both
    sides of the boundary link the same libphi-runtime.so, which is what
    makes a Loop& across it mean one thing.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Wed, 02 Sep 2026 15:10:00 +0200

phi-transport-api (1.6.0-1) trixie; urgency=medium

  * 1.5.0: the envelope moved into the contract. envelope.h carries the frame
    types, the ack/error/cid helpers and dispatchCommand, so a transport takes
    routing from the API instead of keeping a copy that drifts.
  * 1.6.0: a call carries who is making it. CallerIdentity is passed to
    invokeSync/invokeAsync as Anonymous, Session or TrustedLocal; core
    authorizes from that alone, so a client cannot grant itself a capability by
    putting a token in a payload. Transports authenticate their own clients.
  * 1.5.0 was never packaged; this entry covers both steps.
  * PROTOCOLL.md documents the thread model of a core call and the caller
    identity, with the decision log behind them.
 -- Marius Schumacher <marius.schumacher@phisys.com>  Fri, 21 Aug 2026 16:03:45 +0200

phi-transport-api (1.4.0-1) trixie; urgency=medium

  * The plugin contract is Qt-free. TransportInterface is a pure abstract class
    instead of a QObject, and a plugin is a shared object exporting two C entry
    points: phi_transport_api_version() and phi_transport_create(). Q_PLUGIN_METADATA,
    Q_INTERFACES, Q_DECLARE_INTERFACE and the IID are gone, and so is the Qt
    dependency of this package - the whole header set now compiles without Qt.
  * The version gate got stronger, not weaker: phi-core reads the exported version
    string before constructing anything. The old design asked the instance for its
    version, so a mismatched plugin had already been built - and its vtable already
    used - by the time the answer arrived.
  * Identity methods and start()'s error out-parameter are std::string.
  * PHI_TRANSPORT_PLUGIN(Type) exports the entry points. phi-core deletes through
    the virtual destructor, so the plugin's own operator delete frees it.
  * A plugin that wants Qt inherits QObject alongside TransportPluginBase, QObject
    first. The transports phi ships do exactly that; the point is that a third
    party no longer has to.
  * Interface change: kTransportApiVersion is 1.4.0. A 1.3 plugin exports no
    version symbol at all and is refused with a message saying so.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Thu, 20 Aug 2026 18:48:19 +0200

phi-transport-api (1.3.0-1) trixie; urgency=medium

  * LogEntry, Error and their helpers are Qt-free. The diagnostics types were the
    last payload on the boundary still made of QByteArray, QVariantList and
    QJsonObject; they are the schema of a log or error frame, so an implementation
    should be able to read what one contains without Qt. Strings are std::string,
    structured extras are JSON object text, and placeholder values are the new
    Scalar variant.
  * New scalar.h: Scalar/ScalarList plus scalarToJson/scalarListToJson and
    scalarToText. Deliberately the same shape as the adapter contract's
    ScalarValue - the planes never exchange scalars, so this is one idea written
    twice rather than a shared type that would couple them.
  * errorToJson/logEntryToJson return JSON object text, assembled by
    concatenation. errorFromJson/logEntryFromJson are gone: they had no callers,
    and a decoder would mean carrying a JSON parser in a header-only API.
  * New jsonObject() in jsontext.h, so a transport can build structured log
    fields without a JSON library.
  * Interface change: IID moves to /1.3 and kTransportApiVersion to 1.3.0, so
    phi-core rejects a transport built against 1.2 rather than binding it to a
    changed writeLog signature. Both transports and core are rebuilt together.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Thu, 20 Aug 2026 14:56:24 +0200

phi-transport-api (1.2.1-1) trixie; urgency=medium

  * Round-trip tests for LogEntry and Error (transport_types_tests), closing
    F-41. They pin the log vocabulary against the documented table, the incident
    bit through makeCategory/baseCategory and the JSON round trip, 64-bit
    timestamps, translation-parameter types, unknown error flag bits, and that
    decoding never depends on the derived *Name members.
  * Document that an Error without a message deliberately encodes to {}: absence
    of an error is SyncResult::error being empty, not a messageless Error.
  * No interface change: the IID and kTransportApiVersion both stay at 1.2, so
    installed transports keep loading. Documented in the header that the API
    version tracks the interface rather than the package - tying it to the
    package would make every patch release invalidate every installed plugin.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Thu, 20 Aug 2026 11:09:11 +0200

phi-transport-api (1.2.0-1) trixie; urgency=medium

  * Interface break: the data path is UTF-8 JSON text. topic, payloads, config and
    SyncResult::payloadJson are text instead of QString/QJsonObject, so core can
    serialize an event once for all transports and a transport can forward it to its
    wire without parsing. Qt leaves the data path; identity strings and the
    diagnostics types (LogEntry, Error) still use it.
  * Add the Qt-free jsontext.h with the envelope assembly helpers (jsonQuoted,
    jsonField, withJsonField) and a ctest target covering them - escaping, control
    characters, UTF-8 pass-through and the splice cases that would otherwise put a
    malformed frame on the wire (part of F-41).
  * invokeAsync takes the plugin type as a parameter; it used to travel as a
    __phiTransportPluginType key inside the caller's payload (F-40).
  * IID is now .../TransportInterface/1.2 and kTransportApiVersion is 1.2.0.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Wed, 19 Aug 2026 23:51:13 +0200

phi-transport-api (1.1.1-1) trixie; urgency=medium

  * static_assert the shared log numbering (LogLevel, LogCategory ranges) and state
    in PROTOCOLL.md that it is shared with the adapter plane and the wire, so the
    two cannot drift apart again (F-39).

 -- Marius Schumacher <marius.schumacher@phisys.com>  Wed, 19 Aug 2026 23:30:02 +0200

phi-transport-api (1.1.0-1) trixie; urgency=medium

  * Interface break within the v1 line: TransportInterface is stateless and the
    core facade plus the log/call helpers moved to the new header-only
    TransportPluginBase. Transports derive from the base and drop their
    apiVersion() override.
  * Plugin IID is now .../TransportInterface/1.1 and kTransportApiVersion is
    added: phi-core refuses a transport built against another interface version at
    load instead of binding it to a mismatched vtable (F-38). The IID changes even
    though the version stays in the v1 line, because the guard has to be able to
    tell the two builds apart.
  * Version the package by the API version instead of a git snapshot, so
    consumers can express phi-transport-api-dev (>= 1.1.0) as a package relation.
  * Documented that this is a source API with no binary compatibility, and that
    adapters (southbound) and transports (northbound) are told apart by direction
    rather than by protocol name.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Wed, 19 Aug 2026 22:23:17 +0200

phi-transport-api (0.0~git202608192046.3b2b174-1) trixie; urgency=medium

  * Document what CoreFacade::invokeSync's timeout bounds - the handoff to core's
    thread, not the work - and that it must be greater than zero (F-37).

 -- Marius Schumacher <marius.schumacher@phisys.com>  Wed, 19 Aug 2026 20:46:09 +0200

phi-transport-api (0.0~git20260611.0a0111d-1) trixie; urgency=medium

  * Add transport logging hook to the public transport API.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Thu, 11 Jun 2026 23:27:48 +0200

phi-transport-api (0.0~git20260222.f21c775-1) trixie; urgency=medium

  * Initial Debian packaging for phi-transport-api-dev.

 -- Marius Schumacher <marius.schumacher@phisys.com>  Sun, 22 Feb 2026 15:20:00 +0100
